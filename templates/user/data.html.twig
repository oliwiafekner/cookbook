{% include 'default/_form.html.twig' with {
    page_title: 'title.userdata_edit'|trans({'%nick%': user.nick|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'recipe_index',
} %}